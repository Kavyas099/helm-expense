kind: Service
apiVersion: v1
metadata:
  name: frontend
  namespace: expense
  labels:
    project: expense
    tier: frontend
spec:
  type: LoadBalancer
  selector:
    project: expense
    tier: frontend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
    nodePort: 32676
